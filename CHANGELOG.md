# บันทึกการเปลี่ยนแปลง

รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)
เวอร์ชันของ firmware แยกต่างหาก ดูใน repo `paybox-firmware`

## [ยังไม่ปล่อย]

### เปลี่ยนโครงสร้าง
- รวม `paybox-backend` กับ `paybox-control` เป็น repo เดียว โดยเก็บประวัติของทั้งสองไว้
  หน้าเว็บย้ายมาเป็น `frontend/` และ API เป็น `backend/`
- แยกโค้ด PHP ชุดแรก 32 ไฟล์ที่เลิกใช้แล้วออกไปเป็น repo `paybox-legacy` สำหรับอ่านอย่างเดียว
- ปักชื่อ compose project ไว้เป็น `paybox-backend` ตายตัว กันไม่ให้การย้ายโฟลเดอร์ทำให้ชื่อ
  volume เปลี่ยนแล้วฐานข้อมูลดูเหมือนถูกล้าง

### เพิ่ม
- เทสต์ 30 ตัวครอบตรรกะที่แตะเงินและความปลอดภัย: การคิดค่าธรรมเนียม การแบ่งยอด
  การเทียบเวอร์ชัน การตรวจลายเซ็นของ Stripe และ LINE และการ escape CSV
- `Dockerfile` บังคับให้ lint และเทสต์ผ่านก่อนถึงจะได้ image — เป็นด่านที่เลี่ยงไม่ได้
- hook `pre-push` ให้ผลเร็วบนเครื่องก่อนโค้ดจะออกไป
- oxlint กับ `.editorconfig` ทั้งสองฝั่ง

### แก้
- แยก `lib/money.ts` และ `lib/webhookSignature.ts` ออกมาจากโค้ดที่ผูกกับฐานข้อมูลและ HTTP
  เพื่อให้เทสต์ได้ พิสูจน์แล้วว่าสูตรใหม่ให้ผลเท่าเดิมทุกกรณีใน 40,000 กรณีที่ลองเทียบ
- แยกนิยามเมนูออกจาก `PortalSidebar.tsx` เพื่อให้ Fast Refresh ของ Vite ทำงานได้
- เก็บ warning ของ linter ที่ค้างอยู่ให้หมด: import ที่ไม่ได้ใช้, ตัวแปร catch ที่ไม่ได้ใช้,
  ช่องว่างชนิดแปลกในคอมเมนต์, escape ที่ไม่จำเป็นใน regex

## [2569-08-28]

### เพิ่ม
- เอาซอร์สทั้งหมดเข้า git และเปลี่ยนไป deploy ด้วย `git pull` แทนการ scp ไฟล์ทีละไฟล์
- ระบบเฝ้าระวังที่รันเป็น cron บน host ตรวจเว็บ ฐานข้อมูล และ heartbeat ของเครื่องทุก 5 นาที
  แจ้งเตือนออก Telegram และ LINE Messaging API เมื่อสถานะเปลี่ยน
- หน้า "ตั้งค่า > การแจ้งเตือน" สำหรับกรอก credential พร้อมปุ่มทดสอบส่ง ปุ่มค้นหา chat id
  ของ Telegram และ webhook ของ LINE สำหรับหา groupId
- แถบข้างพร้อมปุ่มย่อ ใช้ร่วมกันทั้งพอร์ทัลผู้ดูแลระบบและร้านค้า

### แก้
- `STRIPE_WEBHOOK_SECRET` หายจาก compose เพราะ scp ไฟล์เก่าทับ ทำให้ webhook ตอบ 503
  อยู่ 37 นาที ตรวจแล้วไม่มีรายการใดได้รับผลกระทบ — เป็นเหตุให้เปลี่ยนมา deploy จาก git
