-- เปลี่ยนชื่อคอลัมน์ค่าธรรมเนียมของผู้ให้บริการให้ไม่ผูกกับ Stripe
--
-- ตอนตั้งชื่อครั้งแรกระบบมีผู้ให้บริการเจ้าเดียว ตอนนี้คอลัมน์นี้เก็บค่าธรรมเนียมของ Ksher ด้วย
-- ชื่อเดิมจึงชวนให้เข้าใจผิดว่าเป็นของ Stripe อย่างเดียว
--
-- RENAME COLUMN บน MySQL 8 เป็นการแก้ metadata อย่างเดียว ไม่ต้องเขียนข้อมูลใหม่ทั้งตาราง
-- จึงเสร็จทันทีแม้ตารางจะใหญ่

ALTER TABLE transactions
    RENAME COLUMN stripe_fee_amount TO provider_fee_amount;
